# Archív histórie auditov — Nefro

Plné znenie záznamov z behov auditu **1 až 92** (2026-05-22 až 2026-07-28).
Presunuté sem z [`.audit.md`](.audit.md) v Behu 94, aby playbook ostal čitateľný.
Novšie behy a trvalé poznatky sú v `.audit.md` (sekcie *História auditov* a *Poučenia*).

| Dátum | Záznam |
| --- | --- |
| 2026-08-15 | **Beh 93** — osirelé `ignore` položky v `phpstan-baseline.neon` blokovali analýzu; 42 tabuliek v 21 článkoch obalených do `table-responsive` a 7 `<th>` doplnených o `scope`; pravidlo prenesené do oboch šablón a do `PUBLIKOVANIE_CLANKOV.md`. |
| 2026-07-16 | **Beh 78–82** (hĺbkový audit s opravami do konvergencie) — spevnené e-mailové/newsletter overenie (GET iba potvrdenie, POST+CSRF, hash, riadkový zámok, pevné rate limity, PRG), newsletter subscribe/unsubscribe, logout a citlivé URL; CSP reporty majú limit, whitelist/redakciu, atomický fail-closed zápis a retenčný cleanup. Opravené súbežné použitie mazacieho tokenu, upload avatara (skutočná veľkosť, MIME, chyby, náhodný názov, filesystem až po commite), PRG profilu/resetu a striktné HTTP metódy. Zjednotený CSP-kompatibilný UI skript/pätička a presne jedno `</body></html>`; odstránené inline handlery/štýly, doplnený session warning, bezpečný POST logout, PLASMIC karta a priame odmietnutie voliteľných cookies. Citlivé tokenové stránky potláčajú GA aj referrer; právne texty v2.6 zosúladené na pevné minimum 16 rokov. Formátovanie repa zjednotené. Serverový PDF sync odhalil osamotenú zdrojovú pätičku na takmer prázdnej 4. strane; po kompaktnom zalomení a cielenej regenerácii je PDF renálnej rezervy vizuálne čisté (3 strany, čistý prechod 2→3 aj koniec 3. strany). **Čisté po oprave:** PHP lint 324/0, PHPStan 0, PHP-CS-Fixer 320/0, JS syntax 21/0, `git diff --check` 0, UTF-8/BOM 0, CSP inline vzory 0, POST bez CSRF 0, dokumentové uzávery 0, GA behavior test PASS, CSP endpoint 405/400. Dynamický DB smoke sa neopakoval pre chýbajúci/neaktuálny lokálny fixture; produkčná DB sa pri audite nedotazovala okrem predpísaného idempotentného UPSERTu jedného publikovaného článku (0 vložených, 1 aktualizovaný pri prvej synchronizácii, newsletter 0). |
| 2026-07-16 | **Beh 75–77** (opakovaný hĺbkový audit do konvergencie) — opravené pevné okná rate limitov, osirelé avatary po zlyhanom `INSERT`, chýbajúce upozornenie/predĺženie hodinovej relácie, preferenčné úložisko témy pred súhlasom, mazanie GA cookies po odvolaní, zvyškové používateľské meno a nepravdivý newsletter príznak v audite zrušenia účtu, PII v SMTP/SMS logoch, nepresné tvrdenie o anonymite GA4, chýbajúci label a `noopener`. Právne dokumenty zosúladené na v2.5/`consentVersion` 2026-07-16. **Čisté po oprave:** PHP lint 323/0, PHPStan 0, JS syntax 33/0, UTF-8/BOM 0, `<th>` bez scope 0, neoznačené selecty 0, externé zdroje pred súhlasom 0, admin→robots 0, behaviorálne testy consent/téma/avatar/relácia/právna verzia PASS. Dynamický smoke nebol spustený, lebo dostupná konfigurácia smeruje na nelokálnu DB; produkčná DB sa nemenila. |
| 2026-07-15 | **Beh 72–74** (opakovaný hĺbkový audit podľa `.audit.md`) — **1× GDPR/COOKIE (M):** hosťovská história kalkulačiek sa automaticky zapisovala bez preferenčného súhlasu, uchovávala aj nepoužívané formulárové vstupy s možnými identifikátormi pacienta a chýbala v právnom inventári. Opravené consent-gatingom, minimalizáciou/migráciou záznamov, vymazaním voliteľného úložiska po odvolaní a zosúladením právnych dokumentov (v2.4, `consentVersion` 2026-07-15). **1× ACC:** 41 hlavičiek tabuliek v 7 publikovaných článkoch dostalo `scope="col"`; `search.php` používa jednotný skip cieľ `#main-content`. **1× KÓD:** odstránený mŕtvy titulkový fallback a následne aj jeho zastaraný PHPStan baseline blok. **Čisté po oprave:** PHP lint 316/0, PHPStan 0, JS syntax 32/0, PHP-CS-Fixer na 12 dotknutých PHP súboroch 0, Gitleaks 115 zmenených súborov/0, render 50 verejných stránok/0, `<th>` bez scope 0, externé zdroje pred súhlasom 0, inventár 5 `localStorage` kľúčov pokrytý. Smoke: UTF-8/BOM PASS a login POST 200; ďalší autentifikovaný tok blokuje známy chýbajúci/neaktuálny fixture `test_mobile_verify` (produkčná DB sa nemenila). |
| 2026-07-06 | **Beh 70–71** (opakovaný hĺbkový audit podľa `.audit.md` po pridaní článku o AI a suchej hmotnosti) — **1× ACC:** v 5 publikovaných článkových zdrojoch chýbal `scope="col"` na tabuľkových `<th>`; opravené vo všetkých výskytoch. Dva staršie článkové skripty (`ockovanie-ckd…`, `porovnanie-usmerneni-lupusova-nefritida…`) boli povýšené z `INSERT IGNORE` na bezpečný UPSERT s PDF regeneráciou a newsletterom iba pri novom inserte. **1× ACC/focus:** programaticky fokusované titulky interaktívnych nástrojov (`.tool-step__title`, `.tool-outcome__title`, `tabindex="-1"`) mali základné `outline:none`; doplnený viditeľný focus outline. **Čisté po oprave:** `php .\tools\lint_all.php` 296/0, PHPStan 0, `git diff --check` 0, `<th>` bez scope 0, cross-line úvodzovky 0, UTF-8/BOM 0, interné odkazy 0, consentVersion sync 2026-06-02, admin→robots OK, sitemap nav OK. Smoke: port `8099` je lokálne OS-reserved; ekvivalentný beh na `9000` prejde po login stránku a blokuje ho neaktuálny fixture `test_mobile_verify` (kód nie). |
| 2026-07-05 | **Beh 69** (hĺbková SK jazyková korektúra kódu, článkových zdrojov a DB-publikovateľného obsahu) — opravené: české/nespisovné výrazy (`nesmazal`, `zámerně`, `viď` → `pozri`), preklepy (`rotacíach`, `needopĺňa`), zmiešané úvodzovky v komentároch/UI exportoch, nechcené NBSP v článku „Diabetická choroba obličiek bez záhad“, CSV hlavičky admin exportu s diakritikou. Článkové skripty dotknutých publikovaných textov sú UPSERT/PDF-ready; `add_diabeticka-choroba-obliciek-bez-zahad_article.php` povýšený z `INSERT IGNORE` na bezpečný UPSERT + PDF regeneráciu, newsletter len pri novom inserte. **Čisté:** `php tools/phpstan.phar analyse --no-progress` 0, `php .\tools\lint_all.php` 294 súborov/0 chýb, `git diff --check` 0, zmiešané úvodzovky 0, NBSP ostal len legitímne pri jednotke/počte. **Pozn.:** `php-cs-fixer --dry-run` hlási pre-existing whitespace/blank-line diffs v 9 dotknutých starších PHP súboroch; neaplikované, aby jazyková oprava nebola formátovací sweep. |
| 2026-05-22 | Prvý komplexný audit — 4× H, 8× M, 6× SK, 4× KÓD, 5× RES |
| 2026-05-26 | Všetky nálezy z auditu aplikované a nasadené na produkciu |
| 2026-05-27 | Komplexný audit 20 súborov — 0× H, 1× M, 1× KÓD, 0× SK, 0× RES; žiadne triviálne opravy |
| 2026-05-27 | Audit nových newsletter súborov + prioritných súborov — 0× H, 2× M (1 backlog), 2× KÓD; 2× triviálne opravy aplikované |
| 2026-05-27 | Komplexný audit 20+ súborov — 0× H, 1× M, 1× KÓD, 0× SK, 0× RES; 1× triviálna oprava aplikovaná; backlog #3 (csp-report rate limiting) overený ako hotový |
| 2026-05-27 | M-1 opravený: newsletter_subscribe.php — pridaný Origin/Referer check proti CSRF |
| 2026-05-29 | Komplexný audit 20+ súborov — 0× H, 1× M, 2× KÓD, 4× ACC (skip-link na auth stránkach), 0× SK, 0× RES; 6× triviálnych opráv aplikovaných (skip-link+main-content na login/register/2fa_verify/reset_password/forgot_password, autocomplete="username" v login.php, console.log v ui-preferences.js) |
| 2026-05-30 | Komplexný audit 35 súborov — 0× H, 1× M, 1× KÓD, 0× SK, 0× RES, 5× ACC, 1× GDPR; 6× triviálnych opráv aplikovaných |
| 2026-05-30 | Všetky nálezy opravené: ACC-05 role="status" v 12 kalkulačkách, KÓD-02 cache-busting v newsletter stránkach, GDPR WebSupport SMTP v privacy.php, M-1 SHA-256 hashovanie newsletter tokenov + HMAC unsubscribe URL + DB migrácia |
| 2026-05-30 | Komplexný audit 35+ súborov — 0× H, 0× M, 1× KÓD, 0× SK, 0× RES, 2× ACC; 3× triviálnych opráv aplikovaných (role="status" v calculator_igan+adpkd, box-shadow na .form-row focus, redundantný main_nav include v privacy.php) |
| 2026-05-30 | Komplexný audit 40+ súborov — 0× H, 1× M, 0× SK, 0× RES, 1× ACC (12 kalkulačiek chýbalo aria-live="polite"); M-1 totp_last_counter migrácia chýbala v setup_db.php; 13× triviálnych opráv aplikovaných (aria-live="polite" na 12 kalkulačkách + setup_db.php migrácia) |
| 2026-05-30 | Komplexný audit 45+ súborov (desiaty beh) — 0× H, 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE; 1× KÓD (declare strict*types chýba v 14 calculator*\*.php a calculators_common.php — vyžaduje súhlas); 1× triviálna oprava aplikovaná (htmlspecialchars ENT_QUOTES v profile.php email display) |
| 2026-05-30 | Komplexný audit 45+ súborov (piaty beh) — 0× H, 0× M, 0× SK, 0× RES, 1× ACC (chýbajúci scope="col/row" na <th> vo všetkých tabuľkách); 11× triviálnych opráv aplikovaných (scope="col/row" v admin.php, admin_articles.php, admin_newsletter.php, admin_discussion.php, admin_users_notice.php, calculators_common.php, calculator_history.php, calculator_na.php, calculator_egfr_slope.php, calculator_ckdpc.php, calculator_adpkd.php) |
| 2026-05-30 | Komplexný audit 55+ súborov (šiesty beh) — 1× H (admin_newsletter.php: plaintext verify_token pri resend); 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR; 1× triviálna oprava aplikovaná (H-1 opravená) |
| 2026-05-30 | Komplexný audit 20+ súborov (siedmy beh) — 0× H, 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR; 2× KÓD (index.php dvojité escapovanie $pageTitle, calculator.js localStorage innerHTML); 10× triviálnych opráv (robots.txt 13 chýbajúcich Disallow, profile_form_personal.php autocomplete atribúty, profile_form_address.php autocomplete atribúty) |
| 2026-05-30 | Komplexný audit 50+ súborov (ôsmy beh) — 1× M (calculatorSendSecurityHeaders() prepísala nonce-CSP); 0× H, 0× SK, 0× RES, 0× ACC, 0× GDPR; 1× KÓD ($GLOBALS v calculator_history.php uksort); 4× triviálnych opráv aplikovaných |
| 2026-05-30 | Komplexný audit 40+ súborov (deviaty beh) — 0× H, 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE; 1× KÓD (missing declare(strict_types=1) v article.php, search.php, index.php); 0× triviálnych opráv |
| 2026-05-30 | Komplexný audit 55+ súborov (jedenásty beh) — 0× H, 0× M, 0× SK, 0× RES, 1× ACC (skip-link + main-content ID v verify_email.php), 0× GDPR, 0× COOKIE; 1× KÓD (M-1 chýbajúci declare(strict_types=1) v 11 súboroch); 16× triviálnych opráv aplikovaných (scope="col" 2 tabuľky v admin.php, declare strict_types v calculators.php, calculator_disclaimer.php, sitemap.php, privacy.php, verify_email.php, resend_verification.php, logout.php, phone_utils.php, query_article.php, search_helpers.php, confirm_account_deletion.php, insurance_codes.php, title_codebook.php; autocomplete v profile.php current/new-password) |
| 2026-05-30 | Komplexný audit 50+ súborov (dvanásty beh) — 0× H, 0× M, 0× SK, 0× RES, 6× ACC (skip-link + id="main-content" chýbalo v 2fa_setup.php, calculator_history.php, calculator_uacr.php, profile.php, admin.php, admin_articles.php, admin_discussion.php, admin_newsletter.php), 0× GDPR, 0× COOKIE, 0× KÓD; 8× triviálnych opráv aplikovaných |
| 2026-05-30 | Komplexný audit 60+ súborov (trinásty beh) — 0× H, 0× M, 0× SK, 0× RES, 3× ACC (skip-link+id v admin_users_notice.php a calculator_result_print.php; role="banner" na non-header elemente v discussion.php), 0× GDPR, 0× COOKIE, 0× KÓD; 5× triviálnych opráv aplikovaných |
| 2026-05-30 | Komplexný audit 50+ súborov (štrnásty beh) — 0× H, 2× M (table-name allowlist v auth.php, path traversal v confirm_account_deletion.php), 0× SK, 0× RES, 1× ACC (label for/id v calculator_egfr_slope.php), 0× GDPR, 0× COOKIE, 1× KÓD (newsletter_subscribe.php uses REMOTE_ADDR namiesto getClientIpAddress()); 4× triviálnych opráv aplikovaných (allowlist v auth.php rate-limit funkciách, path traversal fix v confirm_account_deletion.php, autocomplete v resend_verification.php, label for/id v calculator_egfr_slope.php PHP+JS) |
| 2026-05-30 | Komplexný audit 60+ súborov (pätnásty beh) — 0× H, 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE; 2× KÓD (admin_users_notice.php REMOTE_ADDR namiesto getClientIpAddress(); zavádzajúci komentár o SERIALIZABLE v forgot_password.php); 2× triviálnych opráv aplikovaných |
| 2026-05-30 | Komplexný audit 75+ súborov (šestnásty beh) — 0× H, 0× M, 0× SK, 0× RES, 5× ACC (scope="col" chýbal v calculator_ckdpc.php uložené výsledky; label for/id chýbal v calculator_ktv.php 5 polí + calculator_uacr.php 4 polia; autocomplete chýbal v login.php password + register.php 14 polí), 0× GDPR, 0× COOKIE; 3× KÓD (declare strict_types chýbal v archive_cleanup.php, generate_og_image.php, update_sitemap.php); 8 súborov opravených, 0 nálezov vyžadujúcich súhlas |
| 2026-05-30 | Finálny hĺbkový audit 75+ súborov (sedemnásty beh) — 0× H, 0× M, 0× SK, 0× RES, 19× ACC (role="main" chýbalo na <main> elementoch v 19 súboroch), 0× GDPR, 0× COOKIE, 0× KÓD; 20× triviálnych opráv aplikovaných (role="main" v 2fa_setup.php, 2fa_verify.php, admin.php, admin_articles.php, admin_discussion.php, admin_users_notice.php, admin_newsletter.php, calculator_history.php, forgot_password.php, login.php, confirm_account_deletion.php, newsletter_unsubscribe.php, newsletter_verify_sub.php, profile.php, register.php, resend_verification.php, reset_password.php, verify_email.php, search.php) |
| 2026-05-30 | Finálny audit (osemnásty beh) — 1× H (search_helpers.php: buildSearchSnippet() output mal XSS — html_entity_decode bez re-escapovania pred highlightSearchTerms); 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× KÓD; 1× triviálna oprava aplikovaná (htmlspecialchars pred highlightSearchTerms v buildSearchSnippet) |
| 2026-05-30 | Finálny komplexný audit (devätnásty beh) — 0× H, 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× KÓD; 0× triviálnych opráv; všetky escaping vektory overené (search_helpers.php, article.php, discussion.php, admin_articles.php, admin_discussion.php, admin_newsletter.php, profile.php, index.php, head_meta.php, header.php, footer.php, calculator\*.php) — nulové nálezy |
| 2026-06-02 | Právne dokumenty prepracované podľa vzoru alphagrab: nový `legal_data.php` (zdroj pravdy), `privacy.php` prepísané, nové `cookies.php` + `terms.php` (zdravotnícky disclaimer), `legal_head.php` + `legal_related.php`, celosvetové práva po regiónoch (US/CCPA+GPC, LGPD, Ázia, AU/NZ), preferences modal (dátum súhlasu, odkaz na Cookie Policy), `consentVersion` bump 2026-06-02, footer/sitemap aktualizované; `.audit.md` §12 rozšírené na „kontroluje sa pri každom behu" + Self-Improvement Loop + TERMS finding code |
| 2026-06-02 | **Beh 20** (audit právnych dokumentov) — 1× SK (skloňovanie „Slovenská republika" v privacy/terms → `establishment`/`jurisdiction`), 1× GDPR (Google Fonts prenos IP do USA pred súhlasom — doplnené do `legalSubprocessors()` + §5 prenosy); 0× H, 0× M (okrem GDPR), 0× ACC, 0× RES, 0× KÓD; self-improvement: §12 doplnená kontrola vložených tretích strán; odporúčanie: self-hostovať Inter font (vyžaduje súhlas) |
| 2026-06-02 | **Beh 21** (overovací) — 0× nálezov; všetky `<th>` majú scope, aria-labelledby páruje, žiadne mŕtve `governingLaw`, PHP lint čistý, `consentVersion` synchronizovaný; čistý beh |
| 2026-06-03 | **Beh 22** (implementácia GDPR odporúčania) — self-hosting Inter fontu: stiahnutý variabilný Inter (latin + latin-ext woff2, SIL OFL) do `assets/fonts/`, `@font-face` 100–900 v `index.css`; odstránené Google Fonts `<link>`/preconnect z 11 súborov (head*meta, legal_head, 4× admin, print, 3× add*\*, TEMPLATE); CSP `style-src`/`font-src` → `'self'`; `legalSubprocessors()` bez Google Fonts, §5 prenosy + `legalRecentUpdates()` aktualizované, verzia 2.0→2.1; PHP lint čistý. Eliminovaný prenos IP do Google LLC (USA) pred súhlasom |
| 2026-06-03 | **Beh 23** (komplexný audit po Beh 22) — 1× H (refresh_article.php: neautentifikovaný web-prístupný `DELETE FROM articles` bez CLI/admin guardu); 0× M, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× KÓD; 1× oprava aplikovaná (guard `requireAdmin()` pridaný). Overené: žiadne XSS/SQLi vektory (admin_users_notice ORDER BY je allowlist+backtick), žiadne nebezpečné funkcie, žiadny BOM/mojibake, `escapeshellarg` v lint_all.php, `consentVersion` synchronizovaný; self-improvement: §1 doplnená kontrola root skriptov |
| 2026-06-03 | **Beh 24** (timing/CSRF/hash/RES) — 1× M (auth.php `validateJsChallengeToken()` porovnával token cez `===` namiesto `hash_equals()`); 0× H, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× KÓD; 1× oprava aplikovaná (`hash_equals` + null-guard). Overené: `validateCsrfToken` aj newsletter HMAC unsubscribe používajú `hash_equals`/`hash_hmac`, md5 len na názov rate-limit súboru (nie token), RES breakpointy konzistentné (žiadny 1024/960 rozpor) |
| 2026-06-03 | **Beh 25** (ACC/session/cookie/SEO) — 0× nálezov; čistý beh → konvergencia po Beh 23–24. Overené: žiadny `<img>` bez `alt`, session cookies `SameSite=Strict`+`Secure` (podmienené HTTPS), consent cookie `SameSite=Lax`+`Secure` sedí s cookies.php, register.php refaktor zachoval CSRF + upload-až-po-validácii, article.php BreadcrumbList pozície 1≠2 (rôzne URL), žiadne debug leftovers v rozsahu (`tmp_*.js` vylúčené, `display_errors=0` v query_article.php) |
| 2026-06-03 | **Beh 26** (cleanup backlog #4 — zníženie expozície) — `refresh_article.php` doplnený do `.htaccess Require all denied` (čistý CLI skript bez browser UI, konzistencia s `add_search_ft_index.php`/`query_article.php`); 5× mŕtve `tmp_*.js` odstránené z repa (už boli `.htaccess`-blokované); overené, že `tmp_*.js` nikde nereferencované. Zistené: `add_*_article.php`/`add_newsletter_tables_migration.php` majú `requireAdmin()` (admin browser UI — zámer autora, neblokovať), `villages_sk.csv` v `private/` (deny). Backlog #4 ✅ vyriešený. **Pozn. (overené v deploy logu):** deploy hook synchronizuje aj mazanie — `git rm` → `sftp -rm` na serveri; tmp\_\*.js boli teda odstránené aj z produkcie |
| 2026-06-09 | **Beh 28** (audit po session: 3 články, UX registrácia + domovský odberový box, týždenný digest, SEO, čas zverejnenia) — 1× M (inline `style=""` blokované enforcing CSP `style-src 'self'` → presun do tried v index.css: register.php 3×, index.php 3×, article.php 2×), 1× KÓD (robots.txt chýbal Disallow `/newsletter_weekly_digest.php`); 0× H, 0× SK, 0× ACC, 0× GDPR, 0× COOKIE, 0× DB; 5 súborov opravených + nasadené. Overené nové PHP (newsletter_weekly_digest CLI-guard, sendWeeklyNewsletterDigest prepared+htmlspecialchars, admin datetime-local normalizácia, formatArticleDate) — žiadne SQLi/XSS, žiadny nový externý zdroj/subprocessor, consentVersion sync, e-mailové inline štýly OK (CSP sa neaplikuje na mail). **Otvorené M-2:** inline-štýlované `<table>` v obsahu článkov (DB) — vyžaduje súhlas (vizuálna zmena). |
| 2026-06-09 | **Beh 30** (kozmetický inline-style sweep — schválený používateľom) — ~150 statických inline `style=""` v 15 súboroch presunutých do tried v index.css, dávkovo A–D: A verejné kalkulačky+header, B 2FA+profil, C admin (newsletter/diskusia/menu/admin; `$statusColor`→status-triedy), D 15× migračná stránka `add_*_article`. Nové utility + semantické triedy + compound selektory (zhodné renderovanie). Overené: `grep style= *.php` → len legitímne (email_verification, newsletter_notifications = e-mailové HTML; FSGS obsah v DB cez `.primary-article table`); PHP lint čistý, živé verejné stránky 0 inline štýlov, HTTP 200. **Pozn.:** admin/2FA/profil sú autentifikované — konvertované s presnou zhodou tried (bez zámernej vizuálnej zmeny), vhodný letmý vizuálny re-check. |
| 2026-06-09 | **Beh 29** (rozšírenie nálezu Beh 28 — runtime CSP vs inline štýly; konvergencia auto-fixov) — M-2 VYRIEŠENÉ: tabuľky v obsahu článkov (`.primary-article table/th/td` v index.css; FSGS mal 56 blokovaných inline štýlov) + KFRE risk-gauge (dynamická šírka → `data-width` + nonce'd JS). Verejné kalkulačky inak len kozmetické inline štýly (align/margin/farba) — funkčné OK. Ostatné kategórie nového kódu (digest, admin datetime, sitemap) čisté: prepared statements, htmlspecialchars v e-maile, CLI guard, žiadny nový subprocessor. **Backlog (vyžaduje súhlas):** ~150 statických inline štýlov v ~15 súboroch → systematický presun do tried. |
| 2026-06-05 | **Beh 27** (audit po pridaní 7 nových článkov, 2 uhly) — 0× nálezov; čistý beh → konvergencia. Overené: všetkých 7 nových `add_*_article.php` má guard `php_sapi_name()!=='cli' → requireAdmin()` (§1); PHP lint celého repa čistý; žiadny BOM/mojibake; SQLi 0 (admin_users_notice ORDER BY = allowlist+backtick, setup_db/`$pdo->exec` statické reťazce, žiadna konkatenácia s user vstupom); XSS 0 (article.php escapuje title/author/dátum, `content` raw = dôverované admin HTML by-design; theme.js innerHTML statické; katex/qrcode vendor mimo rozsahu); žiadne nebezpečné sinky (include/file ops bez superglobálov, `tools/lint_all.php` má `escapeshellarg`); `consentVersion` sync (2026-06-02 v legal_data.php aj ui-preferences.js); žiadna Google Fonts regresia (len consent-gated GA4/GTM v CSP); hierarchia nadpisov h1(header)→h2(titul+sekcie)→h3 bez preskakovania. **Poučenie:** hromadné pridanie obsahu cez auditovanú `add_TEMPLATE_article.php` šablónu nezavádza regresie — stačí overiť konformitu so šablónou (guard + encoding + typografické úvodzovky), netreba plný re-audit jadra |
| 2026-06-10 | **Beh 31** (audit po pridaní kalkulačky PREVENT — nový PHP kód, nie obsah) — 1× ACC (ACC-01: 5 unit-`<select>` bez prístupného mena — `chol_unit`+`uacr_unit` v PREVENT, `uacr_unit` v KFRE/CKD-PC/KDIGO; WCAG 4.1.2), 1× SEO (neoverená cesta AHA odkazu → indexovaná `about-prevent-calculator`); 0× H, 0× M, 0× SK, 0× RES, 0× GDPR, 0× COOKIE, 0× DB. Opravené: `aria-label="Jednotka …"` na 5 selectoch (4 súbory) + AHA URL. Bezpečnosť nového PREVENT kódu čistá: CSRF `validateCsrfToken`, uloženie cez `calculatorSaveResult` (prepared) za `isLoggedIn()`, XSS 0 (`<option>` ternáre emitujú len literály, hodnoty `$form` cez `htmlspecialchars`); CSP inline-style 0 (gauge = `data-width`+nonce'd JS, správny vzor z Beh 29); `strict_types`/BOM/mojibake/lint čisté; výpočtové jadro overené oproti oficiálnemu príkladu + 600 scenárom (Beh predchádzajúci). Konvergencia: 2. pass bez nových nálezov. **Poučenie:** nová kalkulačka = reálny audit kódu (nie len template-konformita ako pri článkoch); samostatný unit-`<select>` (jednotky) potrebuje `aria-label` — pridať kontrolu do §11. |
| 2026-06-10 | **Beh 32** (komplexný audit + dynamická analýza) — 1× M (SEC: `email_verification.php` SMTP STARTTLS používal `STREAM_CRYPTO_METHOD_TLS_CLIENT` → povoľoval TLS 1.0/1.1, RFC 8996 → vynútené TLS 1.2+, odhalil SonarPHP), 1× M (GDPR: Twilio Inc. (USA, SMS overenie) chýbalo v `legalSubprocessors()` + prenosoch — potvrdené používateľom ako aktívne → doplnené, version 2.1→2.2), 1× SEO (`calculator_prevent.php` chýbal v `sitemap.php` → doplnený), 1× ACC (e-mailové `<html>` bez `lang="sk"` → opravené); 0× H, 0× SK, 0× RES, 0× COOKIE, 0× DB. **Informačné (bez opravy):** dead `$message` plaintext v 2 e-mail funkciách (pôvodný zámer multipart — odporúčaná text+HTML alternatíva, vyžaduje súhlas); `strict_types` chýba v 11 HTML partial-includoch + 3 CLI skriptoch (dlhodobý zámer, >5 súborov, nízka hodnota). **Čisté:** `php -l` celý repo, BOM/mojibake, SQLi (prepared + `{$table}` allowlist), XSS (2 `innerHTML` sinky dokumentované/číselné), 2FA (hash_equals/password_verify/replay/no-early-return — vzorové), CSRF, root guardy, secrets (`.gitignore` kryje `*.env.ini`, žiadne hardcoded), consentVersion sync, breakpointy (hamburger+nav @960), terms disclaimer, select aria-label (recidíva z Beh 31 sa nezopakovala). **Metodologický posun:** plný shell + `php -l` + IDE SonarPHP diagnostika — dynamická analýza nahradila „len statický audit". |
| 2026-06-10 | **Beh 33** (implementácia: prepočty jednotiek + backlog #7/#8) — obsah: do 3 článkov doplnené mmol/l prepočty k mg/dl (cholesterol LDL ÷38,67; glukóza ÷18) + `info-box-blue` poznámky so vzorcami; `add_` skripty prerobené na bezpečný UPSERT (ON DUPLICATE KEY UPDATE, newsletter len pri novom INSERT, `published_at` zachované), spustené cez SSH (1 aktualizovaný/článok, 0 avíz). Backlog #7: multipart/alternative (text+HTML) v `sendViaSmtp()` pre všetkých 5 e-mailov (využitý mŕtvy `$message`, nový plaintext pre verifikáciu/reset; MIME overené). Backlog #8: `strict_types` do 14 partialov/CLI — **smoke-test odhalil latentný fatal** (`patient_insurance_select.php` `htmlspecialchars(int-kľúč)`) → opravené `(string)` cast; re-smoke 15 kalkulačiek + 7 stránok čistých. Všetko nasadené + overené live (curl). |
| 2026-06-11 | **Beh 34** (audit po session: nový CKM článok, oprava nefunkčných odkazov v index/article, ZDS link, FSGS→UPSERT) — **0× nálezov; čistý beh.** Overené: `php -l` celý repo (116 súborov) čistý, žiadny BOM/mojibake, inline `style=` len v 3 legitímnych (FSGS DB obsah + 2 e-maily), SQLi 0 (admin ORDER BY allowlist; SMTP `MAIL FROM` = falošný poplach), CKM článok má CLI/admin guard + UPSERT + typografiu, `consentVersion` sync (2026-06-02), subprocessori (WebSupport/Twilio/Google) prítomní, žiadne externé fonty/preconnect, sitemap úplný (history zámerne vynechaný), `<select>` aria-label bez recidívy, `<img>` všetky s `alt` (avatary `alt=""` vedľa textového mena = WCAG-správne), RES breakpointy @960 konzistentné, `<li>` vyvážené. **Externé odkazy** overené cez DNS-over-HTTPS (Google+Cloudflare): 7 NXDOMAIN/SERVFAIL domén odstránených, presunuté org (ifkf.org, muma.fi) + 404 podstránky → overené fallbacky; 403 publisheri (NEJM/JAMA/lww) = anti-bot, nechané. **Live smoke-test:** domovská/CKM/FSGS HTTP 200, 0 PHP chýb; deploy potvrdený (ZDS live, mŕtve domény preč, ifkf.org/about-us-education live). |
| 2026-06-12 | **Beh 35** (implementácia 10 nálezov) — access log redakcia + historický scrub; atomické 2FA a AEAD šifrovanie s migráciou; HMAC pseudonym pacienta v URL; newsletter unsubscribe POST+CSRF; úplný retenčný cleanup DB/fallback logov; avatar súbory až po DB commite; noindex/canonical bez deletion tokenu; jednotná newsletter odpoveď proti enumerácii; 29× `add_*_article.php` GET-confirm/POST-CSRF; smoke harness zrýchlený a doplnený o assertions. PHP lint + security helper test čisté; smoke korektne odhalil chýbajúci/neaktuálny fixture `test_mobile_verify`. |
| 2026-06-13 | **Beh 36** (audit novej sekcie „Pre pacientov" + DKD článok z DOCX) — 1× ACC (`.badge-popular` mal `--accent-color` #0ea5e9 = ~2,8:1 s bielym textom, pod WCAG AA 4,5:1 → `--primary-color` #2563eb ~4,7:1), 1× CSP (`admin_articles.php` odznak „Pre pacientov" cez inline `style="background:…"` → CSP `style-src 'self'` by orezal → nová trieda `.badge-popular-sm`); 0× H, 0× M, 0× SK, 0× RES, 0× GDPR, 0× COOKIE, 0× DB, 0× POP. 2× oprava aplikovaná + nasadená + overená live. **Čisté:** `php -l` celý repo, `populars.php` (canonical + CollectionPage + BreadcrumbList s rôznymi URL, `htmlspecialchars` na title/excerpt/img-src, karta `alt=""` korektné — odkaz má `aria-label`+viditeľný titul), DKD článok (19 `<img>` všetky s `alt`, klikateľné figúry, disclaimer), `add_*` skripty (CLI/admin guard; publikačné majú `requireAdminMutationConfirmation`). |
| 2026-06-14 | **Beh 39** (overovací po Beh 38) — **0× nálezov; čistý beh → konvergencia.** Live verifikácia gating reťazca: `generate_all_article_pdfs.php`/`pdf_generator.php`/`refresh_article.php` → HTTP 403, priamy `/pdf/*.pdf` → 403, `download_pdf.php` bez loginu → 302 na login.php. Broad sweep: všetky root DB-write skripty majú guard, žiadny priamy `echo $_GET/$_POST`, žiadne token `===` (len legitímny null-guard `$token === ''` pred hash_equals), `hash_equals`/`password_verify` vo všetkých 4 auth-kritických súboroch, `php -l` čistý, git čistý. |
| 2026-06-14 | **Beh 40** (audit po pridaní PHPStan+PHP-CS-Fixer tooling, `@var \PDO $pdo` do 96 súborov, `.htaccess` deny) — 1× M (info-disclosure: `*.md` dokumenty vrátane `.audit.md` web-prístupné → pridané `.htaccess FilesMatch \.md$ Require all denied`); 0× H, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× DB, 0× POP. **Čisté:** `php -l` 134 root súborov, **PHPStan 0 nových chýb nad baseline** (nový nástroj), `@var \PDO $pdo` presne 96:96 (za každým `require db_config.php`), inline `style=` len 3 legitímne (FSGS DB + 2 e-maily), žiadne token `===`, `consentVersion` sync (2026-06-02), 0 SQLi konkatenácie, žiadna Google Fonts regresia, 3 subprocessori (WebSupport/Twilio/Google) prítomní, 0 BOM. **Informačné (vyžaduje súhlas, >5 súborov):** PHP-CS-Fixer hlási 74/134 súborov so štýlovými odchýlkami (kozmetika — ordered_imports, trailing comma, blank lines). |
| 2026-06-14 | **Beh 38** (audit novej PDF infraštruktúry: `pdf_generator.php`, `generate_all_article_pdfs.php`, `download_pdf.php`, admin_articles PDF regenerácia, `sync_article_pdfs.sh`) — 1× M/cleanup (`generate_all_article_pdfs.php` chýbal v `.htaccess Require all denied` — CLI-only skript bez browser UI, bulk GET mení stav → doplnený); 0× H, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× DB, 0× POP. **Čisté:** `php -l` celý repo (134 súborov), žiadny BOM/mojibake, inline `style=` len v 3 legitímnych (FSGS DB obsah + 2 e-maily). `download_pdf.php` vzorové gating (pdf/.htaccess deny + isLoggedIn + slug regex + basename/`.pdf` guard + ob_end_clean pred readfile + nosniff); `pdf_generator.php` web guard + `escapeshellarg` na všetkých shell argoch + slug `preg_replace` sanitizácia + prepared UPDATE; admin PDF regenerácia beží vnútri POST+CSRF (`requireAdmin`+`validateCsrfToken` v admin_articles.php); consentVersion sync (2026-06-02), subprocessori (WebSupport/Twilio/Google) prítomní, žiadny nový externý zdroj (wkhtmltopdf je lokálny), RES @960 konzistentné. Nová PDF funkcia nezavádza tracking ani subprocessora. |
| 2026-06-13 | **Beh 37** (overovací po Beh 36) — **0× nálezov; čistý beh → konvergencia.** `php -l` čistý; žiadny inline `style=` mimo 3 legitímnych (FSGS DB obsah + 2 e-maily); `.badge-popular` na serveri = `--primary-color` (oprava live); live smoke domov/populars/2× článok HTTP 200, 0 PHP chýb. Pozn.: zvyšné `--accent-color` výskyty (`.badge-active` outline, `.btn-admin-action` hover) sú predexistujúce a nie text-na-pozadí — mimo rozsahu. |
| 2026-06-17 | **Beh 41** (audit po ~16 nových článkoch + nový `config_loader.php` + WebP konverzia obrázkov) — 1× KÓD/regресia (**PHPStan 16 NOVÝCH chýb nad baseline**: `nullCoalesce.offset` — `$articles[0]['title'] ?? '(bez titulu)'` v 16 nových `add_*_article.php`; `$articles` je literál s indexom 0 → offset vždy existuje, `??` je mŕtvy kód). Opravené odstránením `?? '(bez titulu)'` len v 16 PHPStan-flagovaných súboroch (staršie nie sú flagované — PHPStan tam offset nevie dokázať; odstránenie by vyvolalo opačnú chybu). PHPStan → 0, `php -l` 151 súborov čistý. **Cosmetic cleanup (schválený, DOKONČENÝ):** 3 články (porovnanie-LN, prehlad-FSGS, ockovanie-CKD) mali inertné inline `style=` na `<table>` v DB obsahu → odstránené zo zdroja; DB vyčistená (prehlad-FSGS cez UPSERT, porovnanie+ockovanie cez jednorazový REPLACE skript, lebo používali `INSERT IGNORE`); `pdf_generator.php` doplnené `table/th/td` pravidlá (PDF nenačítava index.css a inline štýly používali `var(--…)`, ktoré v PDF neexistujú → borders sa nerenderovali); 4 PDF regenerované (`--stale`) + syncnuté. `grep style= *.php` → späť na 3 legit e-maily. **Čisté:** `config_loader.php` (proxy hlavičky za `TRUST_PROXY_HEADERS`, bezpečné generovanie kľúča s race-guardom, 0 injection), guardy na všetkých nových `add_*`, consentVersion sync (2026-06-02), token `===` len legit null-guard, 0 BOM, live smoke 3 články HTTP 200 + 0 PHP chýb. |
| 2026-06-19 | **Beh 42** (opakovaný audit po 8 nových článkoch) — 1× GDPR/M (`legal_data.php` deklaroval GPC, ale `ui-preferences.js` ho neimplementoval) a 1× ACC (spoločná stránka `requireAdminMutationConfirmation()` nemala skip-link). Opravené: GPC vynucuje a perzistuje `marketing: false` pre uložené aj nové voľby, zamyká prepínač a vysvetľuje stav; admin potvrdenie má skip-link ako prvý prvok `<body>`. Druhý beh čistý: PHP lint, PHPStan, JS syntax/behavior, guardy, selecty, sitemap, UTF-8/BOM, článkové HTML a consent/subprocessori. Smoke sa korektne zastavil na známom chýbajúcom/neaktuálnom fixture `test_mobile_verify`; živá DB sa nemenila. |
| 2026-06-19 | **Beh 43** (opakovaný komplexný audit, čistý repo po Beh 42) — 1× M/cleanup (5 CLI-only skriptov s in-code 403 guardom chýbalo v `.htaccess Require all denied`: `newsletter_worker.php`, `newsletter_weekly_digest.php`, `newsletter_apology_broken_links.php`, `archive_cleanup.php`, `update_sitemap.php` — pretrvávalo ~44 commitov); 0× H, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× DB, 0× POP. Doplnené do `.htaccess`, commit+deploy, **live overené HTTP 403**. **Čisté:** `php -l` 159 súborov, PHPStan 0 chýb, JS syntax (3 súbory), inline `style=` len 3 legit (2 e-maily + apology e-mail), 0 SQLi konkatenácie (apology = statický `$pdo->query` + HMAC unsub), token `===` len legit null-guard, sitemap úplný, unit-selecty majú `<label for>`, subprocessori (WebSupport/Twilio/Google), žiadne Google Fonts, consentVersion sync (2026-06-02), 0 BOM/mojibake, `lang="sk"` na vstupných stránkach (legal cez `legal_head.php`). |
| 2026-06-19 | **Beh 44** (overovací po Beh 43) — **0× nálezov; čistý beh → konvergencia.** Live 403 na všetkých 5 nových deny skriptoch, `php -l` + PHPStan + JS syntax čisté, skip-link/lang/ARIA na vstupných stránkach OK. |
| 2026-06-19 | **Beh 46** (riadená práca: sekcia „Zúčastnení autori" + AI-agent pokyny) — **fix+perf+ACC** widgetu autorov (denoise publikácií/časopisov/kompozitných polí cez `parseArticleAuthorField()` + person-filter na všetkých autorov; signature-based cache v `private/cache/`; `aria-expanded`/`aria-controls` na show-more) — live overené (6 reálnych osôb namiesto 17 položiek so šumom, cache aktívna). **Šablóny** `add_TEMPLATE_*` povýšené `INSERT IGNORE`→UPSERT. **Dokumentácia:** `PUBLIKOVANIE_CLANKOV.md` nová sekcia „Pre AI agentov" (pridať/regenerovať/PDF), `PUBLIKOVANIE_PRE_PACIENTOV.md` + `AGENTS.md` aktualizované (+ odstránený zaniknutý `trunk check`). `php -l`/PHPStan/JS čisté. |
| 2026-06-20 | **Beh 47** (riadená práca: rozdelenie byline článku — autor projektu vs. pôvodní autori zdroja) — dokončenie rozpracovanej funkcie z Beh 46. `getArticleBylineAuthors()` odvodí autora projektu (predvolene „MUDr. Ľubomír Polaščín") + zoznam pôvodných autorov zdroja (len osoby) cez `parseArticleAuthorField()` + `extractOriginalSourceFirstAuthor()`; `buildArticleAuthorSchema()` postaví schema.org `author` (jeden Person, alebo pole Person). `article.php`: JSON-LD `author`, meta `article:author` (čistý projektový autor) a viditeľná pätička „Autor: … · Pôvodný autor zdroja: …". **1× latentný bug opravený** (rovnaký ako vo widgete): pole `author` aj citácia `Zdroj:` mohli zapísať tú istú osobu v dvoch notáciách („Batya Swift Yasgur" vs. „Yasgur BS") → citácia použitá **len ako fallback**, keď pole `author` neobsahuje osobu (pre reálne dáta identický výstup — pole `author` je vždy len projektový autor). **1× mŕtvy kód odstránený** (`$articleAuthorRaw` po prepojení nepoužitý). Nový CSS `.source-author`. **Otestované:** 6 prípadov dátovej vrstvy (projekt-only / person+projekt / publikácia-only / zdroj-z-obsahu / dup-projekt-zdroj / prázdny author) + render pätičky (singular/plurál label, `·`, čiarky). `php -l` + PHPStan 0 nad baseline. |
| 2026-06-20 | **Beh 48** (opakovaný audit po 9 nových článkoch + nový `.kilo/` indexačný konfig) — 1× M (info-disclosure: `.kilo/kilo.jsonc` sa servoval na produkcii **HTTP 200**; `hooks/deploy-ignore.txt` vylučoval `.trunk/**`/`.vscode/**` ale NIE `.kilo/**` → nasadené do web rootu a `.htaccess` neblokoval). Opravené v 2 vrstvách: `.kilo/**` do deploy-ignore + `.kilo`/`.trunk` do `.htaccess` RewriteRule deny → **live overené 403**. 0× H, 0× SK, 0× RES, 0× ACC, 0× GDPR, 0× COOKIE, 0× DB, 0× POP. **Čisté:** `php -l` 172 súborov, PHPStan 0 nad baseline, JS syntax (11), všetkých 9 nových `add_*_article.php` template-konformných (CLI/admin guard + `requireAdminMutationConfirmation` + UPSERT + `@var \PDO` + bez mŕtveho `?? '(bez titulu)'` + 0 inline style), inline `style=` len 3 legit (2 e-maily + apology), token `===` 0, 0 BOM/mojibake, consentVersion sync (2026-06-02). **Poučenie:** `.htaccess` deny check je nutný, nie postačujúci — súbor sa na server dostane len ak ho deploy NEvylúči; nový dev/config dir over proti OBOM vrstvám. |
| 2026-06-20 | **Beh 51** (riadená práca: zoznam pôvodných autorov zdroja ku každému článku) — pre 24 článkov vyextrahované plné zoznamy autorov **linkovaného** zdroja a zobrazené vo widgete „Zúčastnení autori" + filtri. **Bez obchádzania paywallov** — len otvorené bibliografické API: AJKD `PII→DOI` cez Crossref `filter=alternative-id:<PII bez ()->`, NEJM/Cureus/NDT podľa DOI/titulu, PMC cez NCBI eutils, verejné tlačové správy (meduniwien/EMJ/ScienceDaily) cez WebFetch. Nový `source_authors.php` (mapa slug→autori, 125 osôb) zlúčený v `articleAuthorIdentities()` → widget aj filter; cache-signatúra + fingerprint mapy; `.htaccess` deny + guard; php -l/PHPStan 0/10 testov; live overené (klik na Praga/Fukui/Jalal=5/Ostermann/Bond → ich články). **Nevyriešené 39** (28× Medscape paywall 402, 4× žurnalistika 403, 7× org/press/video bez menovaných autorov) — ponechané na doplnenie z dát používateľa. |
| 2026-06-20 | **Beh 50** (riadená práca: oprava widgetu „Zúčastnení autori") — **fix mŕtvych odkazov + nezhody počtov.** Widget agregoval autorov podľa odvodenej identity (projekt + zdrojoví autori z byline/„Zdroj:"), ale filter `?autor=` hľadal surovú presnú zhodu `TRIM(author)=:author AND category='odborne'`. Keďže stĺpec `author` = vždy len projektový autor, klik na ktoréhokoľvek zdrojového autora dával „nemá žiadne články" (mŕtve odkazy) a počet projektového autora (všetky publikované) nesedel s filtrom (len odborne). Riešenie: nový `articleAuthorIdentities()` = jednotný zdroj pravdy pre identitu autorov článku, použitý widgetom (počítanie) **aj** filtrom (`fetchPublishedArticlesByAuthor()`, naprieč odborne+popularne) → každý odkaz dopadne na obsah, počty sedia; `registerArticleAuthors` zjednodušený, mŕtvy `registerSourceAuthorOnce` odstránený. Časopisové zdroje (AJKD/NEJM) správne neprejdú person-filtrom (preto nové články nepridávajú menovaných autorov — očakávané). `php -l` + PHPStan 0 + 11 jednotkových testov logiky; **live overené**: klik na Yasgur/Ostermann/Perl/Lin → 1 článok, projekt → 122 (13 strán, = widget). |
| 2026-06-20 | **Beh 49** (overovací po Beh 48) — **0× nálezov; čistý beh → konvergencia.** Live overené: `.kilo/kilo.jsonc` + `.trunk/trunk.yaml` → **403**, `private/` → 403, `pdf/` → 403, homepage/článok → 200; deploy-excluded konfigy (`.pylintrc`, `.yamllint.yaml`, `pyproject.toml`, `extraction_results.json`) → 404; `.gitleaks.toml`/`.php-cs-fixer.dist.php` → host anti-flood throttle (509/466) bez obsahu (deploy-excluded). `php -l` 172 / PHPStan 0 / JS 11 nezmenené (od Beh 48 sa dotklo len `.htaccess`/deploy-ignore/`.audit.md`). |
| 2026-06-24 | **Beh 56** (audit po Fáze 5 — kalkulačky MGRS/KDRI/Gupta, interaktívny sprievodca GN, ťaháky komplement/membranózna) — **1× SK/typografia** (5 zobrazených reťazcov malo zmiešané úvodzovky: správna otváracia „ ale ASCII rovná zatváracia namiesto " — `tick-over`, `spikes`, OPTN KDPI titul, 2× `bland`; opravené, ťaháky re-run UPSERT + PDF regen). 0× H, 0× M, 0× ACC, 0× GDPR, 0× COOKIE, 0× RES, 0× POP. **Čisté:** PHPStan 0 nad baseline, `php -l` všetky nové, §9 `subnav↔sitemap` zhoda (mgrs/kdpi/gupta/nastroj_gn doplnené; len `calculator_history.php` výnimka), inline `style=` len 3 legit e-maily (template komentár „Žiadne inline style…" = FP), unit-selecty (kdpi/gupta) majú `<label for>`, `role=status`/skip/`lang`/`calculator-key` na nových kalkulačkách, BreadcrumbList bez dup URL, consentVersion sync 2026-06-02, žiadne Google Fonts, 0 BOM/mojibake, externé odkazy `rel=noopener`, GN strom 0 mŕtvych `next`. **Live smoke test:** 9 stránok (3 kalkulačky + GN + 2 huby + cheatsheets + 2 články) HTTP 200, 0 PHP chýb. Gupta skóre MAX=22,5 ✓. |
| 2026-06-19 | **Beh 45** (audit po novom článku „mikroplasty-TPE" + nedávnych refactor commitoch) — **2× regresia z refactoru** (botched „Refactor verification…": `resend_verification.php` mal holý `include_once 'header.php';` text mimo PHP, dvojitý head_meta/footer, undefined `$pageTitle`, zmazaný skip-link → obnovená korektná štruktúra; **dvojitý `<title>`** v article/index/populars/search.php — explicitný + z head_meta → odstránený explicitný, head_meta jediný zdroj), **1× KÓD/PHPStan** (18 chýb nad baseline z refactoru: mŕtve `nullCoalesce.offset ??` v db_config author-display + auth `samesite` → odstránené; irreducibilný `$pdo` include-FP v `getAccessLogPdo` → regenerovaný baseline 126 chýb), **2× SK/typografia** v novom článku (`„"` úvodzovky, `≥`/`µl`); 0× H, 0× M, 0× ACC (skip-link obnovený), 0× GDPR, 0× COOKIE. **Čisté:** nový článok konformný (CLI/admin guard + `requireAdminMutationConfirmation` + UPSERT-INSERT IGNORE + `@var \PDO` + bez mŕtveho `?? '(bez titulu)'` + 0 img je OK pre odborný článok), `php -l` 160 súborov, consentVersion sync (2026-06-02), subprocessori (WebSupport/Twilio/Google), žiadne Google Fonts, JS syntax (3), 0 BOM/mojibake, inline `style=` len 3 legit e-maily, token `===` 0. `.tmp-chrome-profile/` → `.gitignore`. |
| 2026-07-03 | **Beh 66** (audit po oprave nefunkčných externých odkazov + 3 nové články: obezita-GLP1 / AI-scribe / TMAO) — **1× SK/typografia:** TMAO článok „proti-TMAO" mal ASCII zatváraciu úvodzovku namiesto U+201C (opravené; DB UPSERT + PDF regen; článok pridaný po Beh 65, recidíva SK-1). 0× H, 0× M, 0× ACC, 0× GDPR, 0× COOKIE, 0× RES, 0× POP, 0× DB. **Riadená práca v tom istom delta:** 6 nefunkčných externých odkazov opravených/odstránených (`uoou.sk`→`dataprotection.gov.sk`, `annuk.org`→`ann-uk.org`, `csn-scn.ca`→`csnscn.ca` +`/_education/`, `www.bvnv.be` expirovaný cert→`bvnv.peepl.be`, `clinicaltrials.ucsf.edu` mŕtvy→`clinicaltrials.gov`, MDCalc PCR 404 odstránený) — detaily v §9 Poučenia. **Čisté:** `php -l` 274, **PHPStan 0 nad baseline**, cross-line úvodzovky 0, BOM 0, inline `style=` len 4 e-mail buildery + template FP, token `===` 0, 3 nové články template-konformné (CLI/admin guard + UPSERT + `@var \PDO` + bez mŕtveho `?? '(bez titulu)'` + `rel=noopener` + 0 inline style), consentVersion sync 2026-06-02, subprocessori nezmenení (WebSupport/Twilio/Google), 0 externých embedov (mapa click-to-load), oprava URL v `legal_data.php` BEZ bump verzie (nie podstatná zmena → nerozosielať). **Live smoke:** 5 stránok (TMAO/FSGS/npcr/privacy/index) HTTP 200, 0 PHP chýb, 1× `<title>`, TMAO živý s U+201C. |
| 2026-07-02 | **Beh 65** (audit po Beh 64: ochrana seedu liekov + 1 odborný článok ADPKD + `source_authors.php`) — **0× nálezov; čistý beh.** Nový obsah plne konformný: `add_vasopresin-nezavisla-cesta-regulacie-vody-adpkd_article.php` (CLI/admin guard + `requireAdminMutationConfirmation` + UPSERT + `@var \PDO` + bez mŕtveho `?? '(bez titulu)'` + 0 inline style + párové úvodzovky „…" U+201E/U+201C: záložná/oddeliť/adjuvans/off-label + `<a rel=noopener>` bez externých embedov), 12 autorov zdroja z otvoreného Crossref API (DOI 10.1172/JCI197021) v `source_authors.php`; `seed_drugs_curation.php` ochrana ručných úprav (default-safe: zverejnené preskočí, doplní len prázdne polia, `--force` = prepis) je SQL-bezpečná (stĺpce z `DG_CUR_COLS` whitelist, bound params), CLI guard + `.htaccess` deny. **Čisté:** `php -l` (article/source_authors/seed), **PHPStan 0 nad baseline**, 0 BOM, inline `style=` len 4 e-mail buildery + template FP, same-line aj cross-line úvodzovky 0 (8 zhôd = PHP docblock/`//` komentáre = FP), token `===` 0 (SMTP kód 220 + TOTP marker = FP), consentVersion sync 2026-06-02, subprocessori (WebSupport/Twilio/Google) nezmenení, článok bez externých embedov (len `<a>` odkazy → žiadny prenos IP pred súhlasom). **Live smoke:** article HTTP 200, 0 PHP chýb, 1× `<title>`, autori zdroja vo widgete + filtri. **Self-improvement:** repo-wide grep úvodzoviek/vzorov MUSÍ `--exclude-dir=.kilo --exclude-dir=node_modules` (Kilo indexer má `node_modules` s minifikovaným vendor JS → 3,1 MB šumu). |
| 2026-07-02 | **Beh 64** (opakovaný hĺbkový audit do konvergencie; repo bez nového obsahu od Beh 63) — **1× GDPR/M (GDPR-1):** `dialyza-bratislava.php` vkladal Google Maps `<iframe>` bezpodmienečne → prenos IP do Google LLC (USA) **pred súhlasom** (rovnaký vzor ako Google Fonts Beh 20; komentár v kóde pritom tvrdil „bez embed iframe"); §12 grep kontrola pokrývala len `<link>/<script>/<img>`, `<iframe>` míňala → opravené **click-to-load** (placeholder `.map-embed-consent` + nonce'd JS vloží iframe až po kliknutí, disclosure + odkaz na zásady Google; frame-src CSP ostáva pre post-click). **1× SK (SK-1, recidíva):** 16 zobrazovaných UI reťazcov so zmiešanými úvodzovkami („ + ASCII ") v 10 súboroch — kalkulačky Fürst/recirkulácia, `cookies.php` 2×, `legal_data.php` 4×, `privacy.php`, `terms.php` 4×, `podpora.php`, `admin_providers.php`, `nastroj_hyponatremia.js` 4×, consent modal `ui-preferences.js` — unikali, lebo kontrola bežala len na nové súbory; opravené presnými literálovými zámenami (bez regexu — poučenie Beh 62), právne stránky BEZ bump verzie (typografia ≠ podstatná zmena; bump by rozoslal e-mail všetkým). **1× SEO (recidíva Beh 58):** `admin_providers.php` chýbal v `robots.txt` Disallow → doplnený. **1× KÓD/cleanup:** e-mail buildery `legal_notifications.php` + `newsletter_notifications.php` chýbali v knižničnom `FilesMatch deny` (sesterská `email_verification.php` tam je) → doplnené, live 403. **Čisté:** `php -l` 273, PHPStan 0, BOM 0, cross-line úvodzovky 0, token `===` 0, SQLi 0 (admin_providers/admin_drugs_review `$where`+`match` = statické fragmenty + bound params, FP), CLI-deny pokrytie, subnav↔sitemap (výnimka len calculator_history), huby v sitemap, consentVersion sync 2026-06-02, GPC implementované, dev diry netrackované, ACC admin_providers (skip/lang/main/scope/aria-label tabuľky). **Live smoke:** 6 stránok 200, buildery 403, mapa bez iframe v initial HTML, 0 PHP chýb. |
| 2026-07-02 | **Beh 63** (dokončenie SK-1 z Beh 62 — cross-line zvyšky) — 8 prózových úvodzoviek so zatváracou ASCII `"` na inom riadku než otváracia „ opravených v DB + tie isté aj v 6 zdrojových `add_*` skriptoch (proti regresii pri re-rune); jednorazový migračný skript po použití odstránený; 5 PDF re-syncnutých zo servera. Deterministická kontrola cross-line: `grep -Pzo '„[^"„“<>]*\n[^"„“<>]*"'`. |
| 2026-07-02 | **Beh 62** (opakovaný hĺbkový audit po pridaní 8 odborných článkov + pacientskej sekcie „Pre pacientov"/Medimpax + `patient_articles_common.php`/`image-fullview.js`) — **KÓD-1:** 22 add-skriptov malo mŕtve `$articles[0]['title'] ?? '(bez titulu)'` (PHPStan `nullCoalesce.offset`) → odstránené; 3 pre-existujúce obranné `alwaysTrue` (`admin_providers.php:456` `if($noteParts)`, `:577` SQL `IN()` guard `$ids!==[]`, `dialyza-bratislava.php:508` email check) baselinované (projekt-konzistentne s podpora.php/prevent_model.php) → **PHPStan 0 nad baseline**. **Pass-2 (session súbory) čisté:** `patient_articles_common.php` escapuje pri priradení (`popRenderCard` htmlspecialchars → žiadne XSS), statický SQL bez user-inputu, `image-fullview.js` `rel=noopener noreferrer`, 0 BOM, 0 inline `style=`, `alt` OK. **SK-1 (recidíva Beh 45/56):** zmiešané úvodzovky — správna otváracia „ (U+201E) ale ASCII rovná zatváracia namiesto „ (U+201C); 62 článkov už správne, **60 článkov/310 výskytov** ASCII → **so súhlasom používateľa** (AskUserQuestion; auto-mode classifier správne zablokoval prvý hromadný apply ako neschválenú produkčnú zmenu) opravené v DB + 108 zdrojov/285 zámien (proti regresii pri re-rune ON DUPLICATE) + 60 PDF regenerovaných `--stale`; live overené (era-2026: 14× U+201C kde predtým 0). Jednorazové migračné skripty po dokončení **odstránené** (nemali admin/CLI guard). **⚠️ KRITICKÉ POUČENIE (vlastná chyba + recovery):** prvá verzia regexu `„([^"<>]*?)"` skonvertovala ASCII " **HTML atribútu** (`alt`/`loading` v `<img>`) na U+201C, keď „ už bola zatvorená cez U+201C — vnútorný span `[^"<>]` povolil existujúcu zatváraciu „ a preskočil cez ňu k zatváracej " atribútu → poškodila 1 článok (`diabeticka-choroba`, re-runy kaskádovo zhoršili). Blast-radius scan (`=“`/`“>`/`““`/`“ attr=`) našiel iba 1; recovery chirurgickým `str_replace` z pristine `<img>` (zdroj INSERT IGNORE → priamy UPDATE). Oprava regexu: vylúčiť z vnútra spanu VŠETKY quote-znaky → `„([^"„“<>]*?)"`. **Ponaučenie:** regex meniaci úvodzovky v HTML obsahu MUSÍ vylúčiť aj „ (U+201E) aj „ (U+201C) z vnútra spanu (nielen ASCII), inak preskočí do atribútu; pri hromadnej regex-transformácii produkčného obsahu VŽDY dry-run + blast-radius scan attr-signatúr PRED aj PO apply, a mutáciu spúšťaj len s explicitným súhlasom. |
| 2026-07-28 | **Beh 92** (opakovaný audit + playbook rename) — Premenovaný bezpečnostný playbook .agent.md → .audit.md; aktualizované odkazy v .doaudit.md, AGENTS.md, legal_data.php, .htaccess, .trunk/trunk.yaml, hooks/deploy-ignore.txt. **KÓD:** PHP-CS-Fixer whitespace korekcie na 106 PHP súboroch. **PHPStan:** 2 baseline položky pre irreducibilný false positive `totp.php:65` (PHPStan neinferuje by-ref GCM tag z `openssl_encrypt`), kód je runtime-korektný. **Smoke test:** lokálne neprejde bez DB pripojenia/fixtures; produkčné overenie po deploy. **Security scan:** 0 H/M, 3× L/anti-pattern (newsletter token overenie cez DB hash lookup, sprintf s hardcoded SQL table names, dynamický ORDER BY s allowlist) — nezmenené, nie sú zraniteľnosti. **Čisté po oprave:** PHP lint 335/0, PHPStan 0 nad baseline, PHP-CS-Fixer 0, UTF-8/BOM PASS, consentVersion sync 2026-07-16. |
| 2026-07-23 | **Beh 91** (používateľské časové pásmo) — Pridaný stĺpec `users.timezone` s predvolenou hodnotou `Europe/Bratislava` (CET/CEST). V profile (`profile.php` + `profile_form_personal.php`) sa dá vybrať z whitelistu IANA časových pásiem (CET/CEST ako default). Prihlásenie (`login.php`, `2fa_verify.php`), synchronizácia relácie (`auth.php`) a uloženie profilu aktualizujú `$_SESSION['timezone']`. Aplikácia nastavuje referenčné pásmo CET/CEST (`date_default_timezone_set('Europe/Bratislava')` v `auth.php` a `db_config.php`) a dátumy/časy sa konvertujú do preferovaného pásma používateľa (`formatUserDateTime`, `formatUserTimestamp`, `formatUserDate`, `getUserTimezoneAbbr`) na verejných stránkach, v článkoch, diskusii, vyhľadávaní, pätičke, profile, admin partneroch, exportoch používateľov, kalkulačkách (max dátum, uložené výsledky) a histórii výpočtov. **Čisté po oprave:** PHP lint 331/0, PHPStan 0. |
| 2026-07-23 | **Beh 90** (article publisher helper) — Nový `article_publisher.php` poskytuje `upsertArticles(PDO, array, category, options)` pre zverejňovanie/regeneráciu článkov: UPSERT do `articles`, pri novom inserte zaradí newsletter avízo (voliteľné), regeneruje PDF. Šablóny `add_TEMPLATE_article.php`, `add_TEMPLATE_popular_article.php` a `add_TEMPLATE_cheatsheet_article.php` boli prevedené na helper. Vybrané existujúce `add_*_article.php` scripty používajú helper, zvyšok zostáva funkčný. **Čisté po oprave:** PHP lint 332/0. |
| 2026-07-23 | **Beh 89** (refresh_article hardening) — `refresh_article.php` web rozlišuje GET/POST: GET zobrazí potvrdzovaciu stránku s CSRF tokenom, POST overí CSRF a až potom maže článok. Web vyžaduje `requireAdmin()` + `requireAdminReauth()`. CLI skript ostáva bez zmeny. **Čisté po oprave:** PHP lint 329/0, PHPStan 0. |
| 2026-07-23 | **Beh 88** (admin re-authentication) — **Nová stránka** `admin_reauth.php` žiada aktuálne heslo, overuje cez `verifySensitiveActionPassword` a nastavuje `_reauthenticated_at`. **Nová funkcia** `requireAdminReauth()` v `auth.php` presmeruje na `admin_reauth.php` ak uplynulo >300 s od posledného overenia. **Citlivé admin akcie** chránené reauth: `admin.php` (`toggle_admin`, `toggle_active`, `reset_password`, `run_cleanup`), `admin_articles.php` (`send_newsletter_queue`, `delete_pending_newsletter`, `delete`), `admin_discussion.php` (všetky moderátorské akcie), `admin_newsletter.php` (všetky subscriber akcie), `admin_providers.php` (`delete`, `toggle_active`, `mark_contacted`, `delete_contact`), `admin_drugs.php` (`toggle_publish`, `delete`), `admin_drugs_review.php` (`toggle_publish`), `admin_trials.php` (`toggle_publish`), `admin_users_notice.php` (citlivý export). **Čisté po oprave:** PHP lint 329/0. |
| 2026-07-23 | **Beh 87** (newsletter/publication hardening) — **SIGTERM/SIGINT:** `newsletter_worker.php` nastavuje `pcntl_signal`; `newsletterShouldShutdown()` s `pcntl_signal_dispatch` kontroluje všetky worker/digest cykly. **SMTP reuse:** `sendViaSmtp` podporuje zdieľaný socket (`$persistentSocket`, `$socketAuthenticated`) a volaný v `processNlSubQueue`, `processArticleNewsletterQueue` a `sendWeeklyNewsletterDigest`; `smtpWritePayloadAndFinish` neposiela `QUIT` pri zdieľanom spojení; `finally`/po cykle sa spojenie riadne uzavrie. **List-Unsubscribe:** newsletter e-maily (fronta, digest) posielajú `List-Unsubscribe` hlavičku pre SMTP aj `mail()` fallback. **PDF WebP fallback:** `pdf_generator.php` konvertuje WebP obrázky na PNG cez GD (`webpToPngFallbackPath`) a odstraňuje vykresľovanie WebP v PDF. **Dynamic OG image:** `article.php` použije prvý vlastný obrázok článku ≥1200 px ako `og:image` a schema.org `image` ak existuje. **PDF tmp:** dočasné HTML pre wkhtmltopdf sa ukladá do `pdf/.tmp` s náhodným názvom. **Čisté po oprave:** PHP lint 328/0, PHPStan žiadne nové chyby (12 pretrvávajúcich). |
| 2026-07-23 | **Beh 86** (admin hardening a audit log) — **Rate limiting:** všetky `admin_*.php` POST handlery používajú `checkFormRateLimit()` (60 pokusov/5 min); citlivý export v `admin_users_notice.php` má vlastný limit 30/5 min. **Admin audit log:** pridané `logAdminAction` pre `admin_articles.php` (create/update/delete/set_top/move/enqueue/requeue/send), `admin_discussion.php` (delete/undelete/hide/unhide/pin/unpin/set_note), `admin_providers.php` (save/delete/toggle/mark_contacted/contact CRUD), `admin_drugs.php` (create/update/toggle/delete), `admin_drugs_review.php` a `admin_trials.php`. **2FA pre admin:** `requireAdmin()` vyžaduje `totp_enabled`; `login.php`, `2fa_verify.php` (cez `completeTwoFactorLogin`) a `synchronizeAuthenticatedSession` nastavujú do session `totp_enabled` a `totp_verified_at`. **Admin LIKE filtre:** `likeSearchPattern()` v `auth.php` escapuje `%`/`_`/`!`/`\`; použité v `admin_trials.php`, `admin_providers.php`, `admin_drugs.php` s `ESCAPE '!'`. **Čisté po oprave:** PHP lint 328/0. |
| 2026-07-23 | **Beh 85** (pokračovanie s ďalšími nálezmi z hĺbkového audita) — **Search:** `search_helpers.php` používa jednotný escape znak `!` v LIKE, FULLTEXT sanitácia whitelistom `\p{L}\p{N}\s`, odstránený nefunkčný `like-normalized` fallback; `setup_db.php` pridáva FULLTEXT index `ft_articles_search` aj idempotentným `ALTER`. **Kalkulačky:** pridaná `calculatorParseNonNegativeFloat()`, `uf_volume` (Kt/V) a albumín (UACR) akceptujú 0; JSON HEX escapovanie v `calculator_history.php` a `calculator_result_print.php`; zjednotené CKD G kategórie do `ckdGCategory`/`ckdGCategoryDescription` v `calculators_common.php`. **Newsletter:** atomický `checkFormRateLimit()` v `auth.php` použitý v `newsletter_subscribe.php` a `newsletter_verify_sub.php`; `sendWeeklyNewsletterDigest()` má throttle 120 e-mailov/min a streamuje `fetch()`; `enqueueArticleNewsletterEmails()` filtruje `cheatsheet`/`tool`; `refresh_article.php` ruší pending frontu a maže PDF; `add_TEMPLATE_article.php` explicitne nastavuje `category='odborne'`; `newsletterRetryDelay()` s exponenciálnym backoffom a jitterom; `last_error` z `smtpGetLastError()`. **Admin:** `admin_newsletter.php` loguje `delete_subscriber`, `force_verify`, `force_unsubscribe`, `resend_verification`, `reactivate`. **Čisté po oprave:** PHP lint 328/0. |
| 2026-07-23 | **Beh 84** (opravy kritickejších nálezov z hĺbkového audita) — **Newsletter secret/TTL:** `newsletter_notifications.php` používa iba `NEWSLETTER_UNSUBSCRIBE_SECRET`, fallback v dev generuje perzistentný náhodný kľúč, max platnosť odkazu znížená na 30 dní, HMAC overuje aj hornú hranicu expirácie. **Admin audit log:** pridaná tabuľka `admin_audit_log` do `setup_db.php` + `logAdminAction()` v `auth.php`; auditované `toggle_admin`, `toggle_active`, `reset_password`, `run_cleanup` v `admin.php` a `delete_pending_newsletter` v `admin_articles.php`; masové mazanie fronty vyžaduje explicitné `confirm_delete_all`. **Refresh article:** `refresh_article.php` parametrizovaný slug + admin/CLI guard, prepared DELETE, audit log. **XSS v JSON výstupoch:** `calc_subnav.php` a `head_meta.php` používajú `JSON_HEX_*` escapovanie. **UPSERT publikovanie:** 47 `add_*_article.php` skriptov konvertovaných z `INSERT IGNORE` na `INSERT ... ON DUPLICATE KEY UPDATE`; newsletter enqueue len pri `rowCount()===1`, pri update sa dohľadá `articleId` pre regeneráciu PDF. **Newsletter queue race:** `processNlSubQueue` a `processArticleNewsletterQueue` obalené transakciou, SELECT s `FOR UPDATE`. **Newsletter verify rate limit:** max pokusov 20→5, blok 15 min→1 h. **Admin providers:** `provider_contacts` počítadlo cez prepared `IN (...)` placeholders; export CSV/JSON/vCard/história cez POST+CSRF. **DB FK:** `codebook_regions` získalo FK na `codebook_countries(code)` (aj idempotentná migrácia). **Čisté po oprave:** PHP lint 330/0, admin→robots OK. |
| 2026-07-22 | **Beh 83** (opakovaný hĺbkový audit do konvergencie nad rozpracovanou funkciou profilovej histórie) — **1× real bug (PDF):** nová href-sanitizácia v `pdf_generator.php` mala `#`-delimiter regex s literálom `#` v triede znakov → `preg_match` zlyhal na compile a z PDF sa strhli VŠETKY relatívne/kotviace `href`; opravené `~`-delimiterom (overené runtime testom). **1× GDPR (consentVersion desync):** hardening validácie súhlasu (`isStoredConsentValid`) bumpol JS `consentVersion` na 2026-07-21, no `legal_data.php` ostal 2026-07-16 → rozpor; keďže šlo len o technický hardening (žiadna zmena rozsahu/podmienok súhlasu — inventár localStorage aj externé zdroje čisté), zosúladené späť na 2026-07-16 (žiadny zbytočný re-consent ani hromadný e-mail); consent test odteraz odvádza verziu zo zdroja (self-syncing). **1× KÓD:** `ckdpcRisk()` `@return` deklaroval kľúč `model` namiesto skutočného `model_name`. **2× SK:** ASCII zatváracia úvodzovka `"` namiesto „ (U+201C) v obsahu 2 článkov (raasi-mra-hyperkaliemia, uti-gsm). Nová funkcia profilovej/avatarovej histórie preverená: allowlist bez senzitívnych polí, prepared statements, utf8mb4, FK `ON DELETE CASCADE` (GDPR výmaz), retenčný cleanup — bez nálezu. **Čisté po oprave:** PHP lint 326/0, JS syntax 21/0, consent test PASS, SK úvodzovky 0, consentVersion SYNCED, BOM 0, `git diff --check` 0, CSP inline len 4 e-mail buildery, admin→robots 9/9, CLI-guard→.htaccess 0, `<th>` bez scope 0, selecty bez labelu 0, externé zdroje pred súhlasom 0, localStorage inventár pokrytý, sitemap úplný. **PHPStan:** lokálne PHP 8.5 sa nezhoduje s 8.3 baseline (skrátené `…` správy nesadnú) → 129 env-drift FP + 5 phar-stub chýb; pretriedené jednotlivo, žiadny nový bug mimo dvoch opravených — spúšťaj PHPStan pod PHP 8.3 (cieľová verzia). |
